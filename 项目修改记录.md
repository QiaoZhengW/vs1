# 医疗影像数据管理平台 - 项目修改记录

**生成日期**：2026年4月15日  
**修改目的**：解决前端登录时出现的 `Failed to fetch` + CORS 跨域错误  
**最终前端运行端口**：5175

## 一、修改文件列表

### 1. `backend/server.js` （最重要修改）
- **修改位置**：第 34-42 行（CORS 配置）
- **修改内容**：大幅增强 CORS 支持

**修改前：**
```js
app.use(
  cors({
    origin: process.env.CORS_ORIGIN?.split(",") || ["http://localhost:5173"],
    credentials: true
  })
);
```

**修改后：**
```js
app.use(
  cors({
    origin: ["http://localhost:5173", "http://localhost:5174", "http://localhost:5175", "http://localhost:5176"],
    credentials: true,
    methods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
    allowedHeaders: ["Content-Type", "Authorization"]
  })
);
```

### 2. `backend/.env` （新建文件）
```env
PORT=4000
DB_HOST=127.0.0.1
DB_PORT=3310
DB_NAME=medical_platform
DB_USER=medical_user
DB_PASSWORD=medical_pass
JWT_SECRET=medical-platform-secret
CORS_ORIGIN=http://localhost:5173
```

### 3. `frontend/.env` （新建文件）
```env
VITE_API_BASE_URL=http://localhost:4000/api
```

## 二、操作记录

- 强制终止了端口 4000 被占用的旧后端进程
- 多次重启后端服务（`npm run dev`）
- 重启前端开发服务器，最终稳定运行在 **5175** 端口
- 验证了后端健康检查接口 `/api/health` 返回正常
- 确认数据库运行在 Docker 容器 `medical-platform-mysql`（端口映射 3310:3306）

## 三、当前启动方式

1. **后端**：在 `backend` 目录下运行 `npm run dev`
2. **前端**：在 `frontend` 目录下运行 `npm run dev`（当前端口为 5175）
3. **数据库**：已通过 Docker 自动运行

**默认登录账号**：
- 管理员：`admin` / `admin123`
- 演示医生：`doctor` / `doctor123`

## 四、访问地址

- 前端：http://localhost:5175
- 后端：http://localhost:4000
- 数据库：localhost:3310（用户名 `medical_user`，密码 `medical_pass`）

---

**文档结束**  
此文件记录了本次会话中对项目的全部修改。如需进一步优化或添加功能，请随时告知。
